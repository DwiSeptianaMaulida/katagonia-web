const express = require('express');const app = express();app.use(express.static('./dist/katagonia-frontend'));app.get('/*', (req, res) =>    res.sendFile('index.html', {root: 'dist/katagonia-frontend/'}),);app.listen(process.env.PORT || 8080);
